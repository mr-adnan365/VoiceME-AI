# Everything needed to run the test suite. The heavy speech models are not
# required: the tests replace them with lightweight fakes.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
httpx>=0.27
pytest>=8
ruff>=0.6
